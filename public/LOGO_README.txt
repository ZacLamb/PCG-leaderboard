Drop your Prestigious Capital Group logo here, named exactly: logo.png

The dashboard loads /logo.png in the header. If the file is missing the
header simply hides the image and shows the text title, so the app still
works without it.
